-- Удаляет таблицу стартовых точек sync_baselines.
--
-- Стартовой точкой сверки стала сама таблица клиентов: план, выпущено, «Договор
-- до» и дата последней выкладки. Отдельная копия этих чисел не нужна.
--
-- Нужно, только если sync_baselines.sql уже запускали. Если нет, запуск ничего
-- не сломает: таблицы просто не окажется.

drop table if exists public.sync_baselines;
